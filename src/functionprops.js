import React from "react";

function functionprops(props){
    return(
        <div><h1>this is functionla component</h1>,
        <h2>this is {props.name} from {props.place}</h2></div>

    ) ;
}
export default functionprops;